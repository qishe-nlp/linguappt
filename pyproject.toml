[tool.poetry]
name = "linguappt"
version = "0.1.3"
description = ""
authors = ["Phoenix Grey <phoenix.grey0108@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
python-pptx = "^0.6.18"
pdf2image = "^1.12.1"
click = "^7.1.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[tool.poetry.scripts]
pptx_validate = 'linguappt.entry.validation:validate'
play_template = 'linguappt.entry.usage:test_template_usage'
vocab_csv2ppt = "linguappt.entry.command:vocab_csv2ppt"
ppt2pdf = 'linguappt.entry.command:ppt2pdf'
vocab_csv2pptpdf = "linguappt.entry.command:vocab_csv2pptpdf"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
